{
  "name": "memory",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check": "tsc --noEmit && sass --no-source-map --quiet src/scss/main.scss /dev/null",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/canvas-confetti": "^1.9.0",
    "sass": "^1.86.0",
    "typescript": "^5.8.3",
    "vite": "^6.2.6"
  },
  "dependencies": {
    "canvas-confetti": "^1.9.4"
  }
}
